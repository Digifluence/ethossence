<div class="contact ethossence-form cart-attributes">

   <div class="field">
      <textarea 
         id="attributes[Needs description]"
         name="attributes[Needs description]"
         class="field__input cart-attribute"
         placeholder="Please describe how you plan to use the products..."
         rows="5"
      >{{ cart.attributes['Needs description'] }}</textarea>
      <label class="field__label" for="attributes[Needs description]">Needs description <span aria-hidden="true">*</span></label>
      <div class="error-message" id="attributes[Needs description]error"></div>
   </div>

   <div class="field">
      <input 
         type="text"
         id="attributes[wc_machine_type]"
         name="attributes[wc_machine_type]"
         class="field__input cart-attribute"
         placeholder="e.g. Column and boom, tube mill, orbital, carriage, robot, etc."
         value="{{ cart.attributes['wc_machine_type'] }}"
      >
      <label class="field__label" for="attributes[wc_machine_type]">What is the type of machine?</label>
      <div class="field__text">e.g. Column and boom, tube mill, orbital, carriage, robot, etc.</div>
   </div>

   <div class="field">
      <input 
         type="text"
         id="attributes[wc_materials_welded]"
         name="attributes[wc_materials_welded]"
         class="field__input cart-attribute"
         placeholder="e.g. Steel, stainless steel, titanium, aluminum, alloys, etc."
         value="{{ cart.attributes['wc_materials_welded'] }}"
      >
      <label class="field__label" for="attributes[wc_materials_welded]">What material(s) will be welded?</label>
      <div class="field__text">e.g. Steel, stainless steel, titanium, aluminum, alloys, etc.</div>
   </div>

   <div class="field">
      <input 
         type="text"
         id="attributes[wc_materials_thickness]"
         name="attributes[wc_materials_thickness]"
         class="field__input cart-attribute"
         placeholder="Welding applications only, please indicate if inches or mm"
         value="{{ cart.attributes['wc_materials_thickness'] }}"
      >
      <label class="field__label" for="attributes[wc_materials_thickness]">What is the thickness of the material(s)?</label>
      <div class="field__text">Welding applications only, please indicate if inches or mm.</div>
   </div>

   <div class="field">
      <input 
         type="text"
         id="attributes[wc_shielding_gases]"
         name="attributes[wc_shielding_gases]"
         class="field__input cart-attribute"
         placeholder="e.g. argon, oxygen, CO2, etc."
         value="{{ cart.attributes['wc_shielding_gases'] }}"
      >
      <label class="field__label" for="attributes[wc_shielding_gases]">Which shielding gases are used?</label>
      <div class="field__text">e.g. argon, oxygen, CO2, etc.</div>
   </div>

   <div class="field">
      <input 
         type="text"
         id="attributes[wc_power_avg]"
         name="attributes[wc_power_avg]"
         class="field__input cart-attribute"
         placeholder="Please indicate amps/KW and if welding is pulsed"
         value="{{ cart.attributes['wc_power_avg'] }}"
      >
      <label class="field__label" for="attributes[wc_power_avg]">What is the average welding power?</label>
      <div class="field__text">Please indicate amps/KW and if welding is pulsed.</div>
   </div>

   <div class="field">
      <input 
         type="text"
         id="attributes[wc_power_peak]"
         name="attributes[wc_power_peak]"
         class="field__input cart-attribute"
         placeholder="Please indicate amps/KW and if welding is pulsed"
         value="{{ cart.attributes['wc_power_peak'] }}"
      >
      <label class="field__label" for="attributes[wc_power_peak]">What is the peak welding power?</label>
      <div class="field__text">Please indicate amps/KW and if welding is pulsed.</div>
   </div>

   <div class="cart-save-draft" style="margin: 2rem 0; text-align: center;">
      <button id="save-cart-draft" type="button" class="button" data-customer-id="{{ customer.id }}" data-customer-email="{{ customer.email }}">
         {{ bth_submit_account |  default: 'Submit for Review' }}
      </button>
      <div id="save-cart-message" style="margin-top: 1rem; display: none;"></div>
   </div>

</div>